# Read-only foundation delivered (Lessons 0001–0005) — covered, not yet verified

Lessons 0001–0005 have all been delivered: navigation (`pwd`/`ls`/`cd`), SSH into the NAS, reading files (`cat`/`less`/`head`/`tail`), finding things (`find`/`grep`), and getting help (`man`/`--help`/`apropos`). Together they form a complete **read-only exploration toolkit** — the user can look around any Linux system, including the NAS, without changing anything.

**Status:** covered, *not yet evidenced*. The user has received these lessons but has not yet demonstrated recall or application, so this record deliberately does **not** claim mastery — per the principle that coverage is not learning.

**Implication:** the next real milestone is either (a) the [interleaved review](../reviews/0001-interleaved-lessons-1-5.html) to generate evidence and upgrade this into per-skill mastery records, or (b) Lesson 0006 — the first commands that *change* files. A future session should verify rather than assume this foundation is retained.
